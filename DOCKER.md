# 🐳 Docker конфигурация для Credit Scoring

## 📋 Описание

Проект настроен для запуска в Docker контейнерах с использованием Docker Compose.

## 🏗️ Архитектура

- **backend**: FastAPI приложение (порт 8000)
- **frontend**: Streamlit приложение (порт 8501)
- **volumes**: данные, модели, отчёты и база данных

---

## 🚀 Быстрый старт

### 1. Клонирование и подготовка

```bash
# Клонирование репозитория (если нужно)
git clone <repository-url>
cd credit_scoring

# Создание .env файла (опционально)
cp env.example .env
# Отредактируйте .env и измените SECRET_KEY и другие настройки
```

### 2. Сборка и запуск

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### 3. Доступ к приложению

- **Backend API**: http://localhost:8000
- **Frontend (Streamlit)**: http://localhost:8501
- **API документация (Swagger)**: http://localhost:8000/docs
- **API документация (ReDoc)**: http://localhost:8000/redoc

---

## 📝 Управление контейнерами

### Просмотр статуса

```bash
# Статус всех сервисов
docker-compose ps

# Логи всех сервисов
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend
docker-compose logs frontend

# Логи в реальном времени
docker-compose logs -f
```

### Остановка и запуск

```bash
# Остановка всех сервисов
docker-compose stop

# Запуск остановленных сервисов
docker-compose start

# Остановка и удаление контейнеров
docker-compose down

# Остановка, удаление контейнеров и volumes
docker-compose down -v
```

### Пересборка

```bash
# Пересборка без кэша
docker-compose build --no-cache

# Пересборка и перезапуск
docker-compose up --build -d
```

---

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` на основе `env.example`:

```bash
cp env.example .env
```

Основные переменные:

- `SECRET_KEY`: Секретный ключ для JWT (обязательно изменить в production!)
- `ACCESS_TOKEN_EXPIRE_MINUTES`: Время жизни access токена (по умолчанию: 30)
- `REFRESH_TOKEN_EXPIRE_DAYS`: Время жизни refresh токена (по умолчанию: 7)
- `API_BASE_URL`: URL backend API (по умолчанию: http://localhost:8000)
- `HOST`: Хост для backend (по умолчанию: localhost)
- `PORT`: Порт для backend (по умолчанию: 8000)

### Volumes

Следующие директории монтируются как volumes:

- `./data` → `/app/data` - данные
- `./models` → `/app/models` - обученные модели
- `./reports` → `/app/reports` - PDF отчёты
- `./credit_scoring.db` → `/app/credit_scoring.db` - база данных SQLite
- `./credit_scoring.log` → `/app/credit_scoring.log` - логи приложения

---

## 🗄️ База данных

### Применение миграций

Миграции применяются автоматически при запуске backend контейнера через entrypoint скрипт.

Для ручного применения:

```bash
# Вход в backend контейнер
docker-compose exec backend bash

# Применение миграций
alembic upgrade head
```

### Создание пользователей

Пользователи создаются автоматически при первом запуске через entrypoint скрипт.

Для ручного создания:

```bash
# Вход в backend контейнер
docker-compose exec backend bash

# Создание пользователей
python scripts/create_users.py
```

Создаются пользователи:
- `admin` / `admin123` (роль: admin)
- `analyst` / `analyst123` (роль: analyst)
- `user` / `user123` (роль: user)

---

## 🔍 Отладка

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend
docker-compose logs frontend

# Логи в реальном времени
docker-compose logs -f backend
```

### Вход в контейнер

```bash
# Backend контейнер
docker-compose exec backend bash

# Frontend контейнер
docker-compose exec frontend bash
```

### Проверка здоровья

```bash
# Проверка статуса healthcheck
docker-compose ps

# Проверка вручную
curl http://localhost:8000/  # Backend
curl http://localhost:8501/_stcore/health  # Frontend
```

---

## 🛠️ Разработка

### Режим разработки

Для разработки можно использовать volumes для монтирования кода:

```yaml
# В docker-compose.yml добавьте volumes для кода
volumes:
  - .:/app
```

### Горячая перезагрузка

FastAPI автоматически перезагружается при изменении кода (если включен `--reload`).

Для Streamlit также доступна автоперезагрузка.

---

## 📦 Структура файлов

```
credit_scoring/
├── Dockerfile.backend          # Dockerfile для backend
├── Dockerfile.frontend         # Dockerfile для frontend
├── docker-compose.yml          # Docker Compose конфигурация
├── docker-entrypoint-backend.sh    # Entrypoint для backend
├── docker-entrypoint-frontend.sh   # Entrypoint для frontend
├── .dockerignore               # Исключения для Docker build
├── env.example                 # Пример переменных окружения
└── DOCKER.md                   # Эта документация
```

---

## ⚠️ Важные замечания

### Безопасность

1. **Обязательно измените SECRET_KEY** в `.env` файле для production!
2. Используйте HTTPS в production окружении
3. Не коммитьте `.env` файл в репозиторий

### Производительность

1. SQLite подходит для разработки, но для production рекомендуется PostgreSQL
2. Для production используйте более мощные ресурсы контейнеров

### Данные

1. Все данные хранятся в volumes и сохраняются между перезапусками
2. При удалении volumes (`docker-compose down -v`) все данные будут удалены!

---

## 🔄 Обновление

### Обновление кода

```bash
# Остановка контейнеров
docker-compose down

# Обновление кода (git pull и т.д.)
git pull

# Пересборка и запуск
docker-compose up --build -d
```

### Обновление зависимостей

```bash
# Обновление requirements.txt
# ...

# Пересборка
docker-compose build --no-cache

# Перезапуск
docker-compose up -d
```

---

## 📚 Дополнительные ресурсы

- [Docker документация](https://docs.docker.com/)
- [Docker Compose документация](https://docs.docker.com/compose/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Streamlit документация](https://docs.streamlit.io/)

---

**Дата обновления**: 2025-01-27  
**Версия**: 1.0.0

