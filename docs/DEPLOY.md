# 🚀 Руководство по деплою на PythonAnywhere

## 📋 Описание

Это руководство описывает процесс настройки CI/CD для деплоя приложения Credit Scoring на PythonAnywhere.

---

## 🔧 Настройка GitHub Actions

### 1. Настройка Secrets в GitHub

Перейдите в настройки репозитория: **Settings → Secrets and variables → Actions**

Добавьте следующие секреты:

- **`PYTHONANYWHERE_SSH_KEY`**: Приватный SSH ключ для доступа к PythonAnywhere
- **`PYTHONANYWHERE_USER`**: Ваш username на PythonAnywhere
- **`PYTHONANYWHERE_HOST`**: SSH хост (обычно `ssh.pythonanywhere.com`)
- **`PYTHONANYWHERE_PATH`**: Путь к проекту на сервере (например, `~/credit_scoring`)

### 2. Генерация SSH ключа

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "github-actions@pythonanywhere" -f ~/.ssh/pythonanywhere_key

# Публичный ключ нужно добавить на PythonAnywhere
cat ~/.ssh/pythonanywhere_key.pub
```

**Добавление ключа на PythonAnywhere:**

1. Войдите в консоль PythonAnywhere
2. Перейдите в раздел Account → SSH keys
3. Добавьте публичный ключ

**Приватный ключ добавьте в GitHub Secrets как `PYTHONANYWHERE_SSH_KEY`**

---

## 🏗️ Первоначальная настройка на PythonAnywhere

### 1. Подготовка сервера

```bash
# Установите переменные окружения локально
export PYTHONANYWHERE_USER="your_username"
export PYTHONANYWHERE_HOST="ssh.pythonanywhere.com"
export PYTHONANYWHERE_PATH="~/credit_scoring"

# Запустите скрипт настройки
chmod +x scripts/setup_pythonanywhere.sh
./scripts/setup_pythonanywhere.sh
```

### 2. Настройка WSGI файла

На PythonAnywhere:

1. Перейдите в **Web → WSGI configuration file**
2. Замените содержимое на:

```python
# WSGI файл для PythonAnywhere
import sys
import os

# Путь к проекту
project_home = os.path.expanduser('~/credit_scoring')
if project_home not in sys.path:
    sys.path.insert(0, project_home)

# Активируем виртуальное окружение
activate_this = os.path.expanduser('~/credit_scoring/venv/bin/activate_this.py')
if os.path.exists(activate_this):
    with open(activate_this) as f:
        exec(f.read(), {'__file__': activate_this})

# Импортируем приложение
from app.main import app

application = app
```

### 3. Настройка статических файлов

На PythonAnywhere:

1. Перейдите в **Web → Static files**
2. Добавьте:
   - **URL**: `/static`
   - **Directory**: `/home/YOUR_USERNAME/credit_scoring/static`

### 4. Настройка переменных окружения

На PythonAnywhere создайте файл `.env`:

```bash
cd ~/credit_scoring
nano .env
```

Содержимое:

```env
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
API_BASE_URL=https://your_username.pythonanywhere.com
HOST=0.0.0.0
PORT=8000
```

### 5. Перезагрузка приложения

На PythonAnywhere:

1. Перейдите в **Web → Reload**
2. Нажмите кнопку **Reload**

---

## 🔄 Автоматический деплой через GitHub Actions

### Настройка

1. **Настройте Secrets** (см. раздел выше)
2. **Настройте ветку**: По умолчанию деплой происходит при push в `main`
3. **Push в main ветку** — деплой запустится автоматически

### Процесс деплоя

GitHub Actions автоматически выполняет следующие шаги:

#### 1. Pre-Deploy Checks (проверки перед деплоем)

1. ✅ **Линтинг кода** (Black, Flake8, Pylint)
2. ✅ **Проверка формата кода** (Black)
3. ✅ **Запуск тестов** (pytest с покрытием кода)
4. ✅ **Проверка импортов** (валидация всех модулей)
5. ✅ **Проверка миграций Alembic**
6. ✅ **Сборка Docker образов** (backend и frontend)
7. ✅ **Проверка Docker образов**

#### 2. Deploy (деплой на PythonAnywhere)

Только если все проверки прошли успешно:

1. ✅ Подключается к PythonAnywhere через SSH
2. ✅ Обновляет код из git (pull из main ветки)
3. ✅ Устанавливает/обновляет зависимости
4. ✅ Применяет миграции базы данных
5. ✅ Создаёт/обновляет пользователей
6. ✅ Перезагружает веб-приложение
7. ✅ Проверяет успешность деплоя (импорт приложения)

### Условия запуска деплоя

Деплой запускается **только если**:
- ✅ Все тесты прошли успешно
- ✅ Все линтеры прошли проверку
- ✅ Docker образы собрались без ошибок
- ✅ Все проверки импортов прошли

Если любая проверка не прошла, деплой **не запускается**.

---

## 🛠️ Ручной деплой

### Использование скрипта деплоя

```bash
# Установите переменные окружения
export PYTHONANYWHERE_USER="your_username"
export PYTHONANYWHERE_HOST="ssh.pythonanywhere.com"
export PYTHONANYWHERE_PATH="~/credit_scoring"

# Запустите деплой
chmod +x scripts/deploy_pythonanywhere.sh
./scripts/deploy_pythonanywhere.sh
```

### Через SSH вручную

```bash
# Подключение к серверу
ssh your_username@ssh.pythonanywhere.com

# Обновление кода
cd ~/credit_scoring
git fetch origin
git reset --hard origin/main

# Активация виртуального окружения
source venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

# Применение миграций
alembic upgrade head

# Создание пользователей
python scripts/create_users.py

# Перезагрузка приложения (на веб-интерфейсе или через touch)
touch /var/www/your_username_pythonanywhere_com_wsgi.py
```

---

## 📊 Мониторинг деплоя

### GitHub Actions

- Проверьте статус деплоя в **Actions** вкладке репозитория
- Логи доступны для каждого запуска

### PythonAnywhere

- Проверьте логи в **Web → Error log**
- Проверьте логи приложения в `credit_scoring.log`

---

## 🔍 Отладка

### Проблемы с SSH

```bash
# Проверка подключения
ssh -v your_username@ssh.pythonanywhere.com

# Проверка SSH ключа
ssh-add -l
```

### Проблемы с зависимостями

```bash
# На PythonAnywhere
cd ~/credit_scoring
source venv/bin/activate
pip list
pip install -r requirements.txt --force-reinstall
```

### Проблемы с базой данных

```bash
# На PythonAnywhere
cd ~/credit_scoring
source venv/bin/activate
alembic current
alembic history
alembic upgrade head
```

### Проблемы с WSGI

1. Проверьте путь к проекту в WSGI файле
2. Проверьте активацию виртуального окружения
3. Проверьте импорт приложения

---

## 🔐 Безопасность

### Рекомендации

1. **Измените SECRET_KEY** в `.env` файле на production
2. **Не коммитьте `.env`** файл в репозиторий
3. **Используйте HTTPS** для API
4. **Регулярно обновляйте зависимости**
5. **Используйте сильные пароли** для пользователей

---

## 📝 Структура файлов

```
credit_scoring/
├── .github/
│   └── workflows/
│       ├── ci.yml              # CI workflow (тестирование, линтинг)
│       └── deploy.yml          # Deploy workflow (деплой на PythonAnywhere)
├── scripts/
│   ├── deploy_pythonanywhere.sh    # Скрипт ручного деплоя
│   └── setup_pythonanywhere.sh     # Скрипт первоначальной настройки
├── wsgi.py                     # WSGI файл для PythonAnywhere
└── DEPLOY.md                    # Эта документация
```

---

## 🚨 Важные замечания

### Ограничения PythonAnywhere

1. **Бесплатный аккаунт**: ограниченное время выполнения скриптов
2. **Файловая система**: ограничения на запись в некоторые директории
3. **База данных**: SQLite работает, но для production рекомендуется PostgreSQL

### Рекомендации для production

1. **Используйте платный аккаунт** для production
2. **Настройте регулярные бэкапы** базы данных
3. **Мониторинг логов** приложения
4. **Настройте алерты** на ошибки

---

## 📚 Дополнительные ресурсы

- [PythonAnywhere документация](https://help.pythonanywhere.com/)
- [GitHub Actions документация](https://docs.github.com/en/actions)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Alembic документация](https://alembic.sqlalchemy.org/)

---

**Дата обновления**: 2025-01-27  
**Версия**: 1.0.0

