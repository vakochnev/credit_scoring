# 🚀 Быстрый старт с Docker

## Установка и запуск

### 1. Предварительные требования

- Docker (версия 20.10+)
- Docker Compose (версия 1.29+)

Проверка установки:
```bash
docker --version
docker-compose --version
```

### 2. Клонирование и подготовка

```bash
# Клонирование репозитория (если нужно)
git clone <repository-url>
cd credit_scoring

# Создание .env файла (опционально, но рекомендуется)
cp env.example .env
# Отредактируйте .env и измените SECRET_KEY
```

### 3. Запуск приложения

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### 4. Доступ к приложению

После запуска доступны:
- **Backend API**: http://localhost:8000
- **Frontend (Streamlit)**: http://localhost:8501
- **API документация**: http://localhost:8000/docs

### 5. Первый вход

При первом запуске автоматически:
- ✅ Применяются миграции базы данных
- ✅ Создаются пользователи

Созданные пользователи:
- `admin` / `admin123` (роль: admin)
- `analyst` / `analyst123` (роль: analyst)
- `user` / `user123` (роль: user)

---

## Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Остановка и удаление
docker-compose down

# Просмотр логов
docker-compose logs -f

# Пересборка
docker-compose up --build -d

# Статус
docker-compose ps
```

---

## Устранение проблем

### Проблемы с портами

Если порты 8000 или 8501 заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "8001:8000"  # Измените 8000 на 8001
```

### Проблемы с базой данных

Если база данных не создаётся:

```bash
# Удаление volumes и пересоздание
docker-compose down -v
docker-compose up -d
```

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Логи backend
docker-compose logs backend

# Логи frontend
docker-compose logs frontend
```

---

## Дополнительная информация

Подробная документация: [DOCKER.md](DOCKER.md)

