#!/bin/bash
# Entrypoint скрипт для backend контейнера

set -e

echo "🚀 Запуск backend контейнера..."

# Ожидание готовности базы данных
echo "⏳ Ожидание готовности базы данных..."
sleep 2

# Применение миграций Alembic
echo "📦 Применение миграций базы данных..."
alembic upgrade head || echo "⚠️ Ошибка при применении миграций (возможно, БД уже обновлена)"

# Создание пользователей (если нужно)
echo "👤 Проверка пользователей..."
python scripts/create_users.py || echo "⚠️ Ошибка при создании пользователей"

# Запуск приложения
echo "✅ Запуск FastAPI сервера..."
exec uvicorn app.main:app --host 0.0.0.0 --port 8000

