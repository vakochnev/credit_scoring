# 🔐 Улучшения безопасности системы

## Обзор изменений

Система кредитного скоринга была улучшена с добавлением:
- **JWT токены** (access и refresh) вместо HTTP Basic Auth
- **Система ролей** пользователей (admin, analyst, user)
- **Разграничение доступа** по ролям
- **Логирование действий** пользователей

---

## 🎯 Новые возможности

### 1. JWT токены
- **Access токен**: короткий срок жизни (30 минут)
- **Refresh токен**: длинный срок жизни (7 дней)
- Токены хранятся на клиенте и передаются в заголовке `Authorization: Bearer <token>`

### 2. Система ролей

#### Роль: `admin`
- ✅ Полный доступ ко всем функциям
- ✅ Обучение моделей (`/train-final`)
- ✅ Дообучение моделей (`/retrain`)
- ✅ Сравнение моделей (`/compare`)
- ✅ Генерация отчётов
- ✅ Просмотр фидбэков

#### Роль: `analyst`
- ✅ Прогнозирование (`/predict`, `/explain`)
- ✅ Генерация PDF-отчётов (`/report`)
- ✅ Сравнение моделей (`/compare`)
- ✅ Дообучение моделей (`/retrain`)
- ✅ Просмотр фидбэков

#### Роль: `user`
- ✅ Базовое прогнозирование (`/predict`)
- ✅ Объяснение решений (`/explain`)
- ✅ Сохранение фидбэков (`/feedback`)
- ❌ Нет доступа к обучению, отчётам, сравнению моделей

---

## 📡 Новые эндпоинты API

### POST `/login`
Авторизация и получение JWT токенов.

**Запрос:**
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### POST `/refresh`
Обновление access токена используя refresh токен.

**Запрос:**
```json
{
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**Ответ:** Новые access и refresh токены (формат как в `/login`)

### GET `/me`
Получение информации о текущем пользователе.

**Заголовок:**
```
Authorization: Bearer <access_token>
```

**Ответ:**
```json
{
  "id": 1,
  "username": "admin",
  "role": "admin",
  "is_active": true
}
```

---

## 🔄 Изменения в существующих эндпоинтах

Все эндпоинты теперь требуют JWT токен в заголовке:
```
Authorization: Bearer <access_token>
```

Доступ к эндпоинтам теперь зависит от роли пользователя (см. раздел "Система ролей").

---

## 🚀 Установка и настройка

### 1. Установка зависимостей
```bash
pip install python-jose[cryptography] python-multipart
```

### 2. Применение миграции базы данных
```bash
alembic upgrade head
```

### 3. Создание пользователей
```bash
python scripts/create_users.py
```

Создаются пользователи:
- `admin` / `admin123` (роль: admin)
- `analyst` / `analyst123` (роль: analyst)
- `user` / `user123` (роль: user)

---

## 📝 Примеры использования

### Python (requests)
```python
import requests

# Авторизация
response = requests.post(
    "http://localhost:8000/login",
    json={"username": "admin", "password": "admin123"}
)
tokens = response.json()

# Использование токена
headers = {"Authorization": f"Bearer {tokens['access_token']}"}
response = requests.post(
    "http://localhost:8000/predict",
    json={...},
    headers=headers
)
```

### cURL
```bash
# Авторизация
TOKEN=$(curl -X POST http://localhost:8000/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | jq -r '.access_token')

# Использование токена
curl -X POST http://localhost:8000/predict \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{...}'
```

---

## ⚙️ Конфигурация

Настройки JWT находятся в `shared/config.py`:
- `SECRET_KEY`: секретный ключ для подписи токенов (генерируется автоматически)
- `ALGORITHM`: алгоритм шифрования (HS256)
- `ACCESS_TOKEN_EXPIRE_MINUTES`: время жизни access токена (30 минут)
- `REFRESH_TOKEN_EXPIRE_DAYS`: время жизни refresh токена (7 дней)

---

## 🔒 Безопасность

### Реализовано:
- ✅ JWT токены с подписью
- ✅ Разделение access и refresh токенов
- ✅ Проверка ролей на уровне API
- ✅ Хеширование паролей (bcrypt)
- ✅ Деактивация пользователей
- ✅ Логирование действий

### Рекомендации для production:
- 🔐 Использовать HTTPS для передачи токенов
- 🔐 Хранить SECRET_KEY в переменных окружения
- 🔐 Реализовать rate limiting
- 🔐 Добавить проверку на отозванные токены (blacklist)
- 🔐 Регулярно обновлять пароли пользователей

---

## 📚 Дополнительная информация

- **Модель User**: `shared/models.py`
- **Модуль авторизации**: `shared/auth.py`
- **Эндпоинты API**: `app/main.py`
- **Миграция БД**: `alembic/versions/a1b2c3d4e5f6_add_roles_and_jwt_support.py`

---

**Дата обновления**: 2025-01-27  
**Версия API**: 2.0.0

